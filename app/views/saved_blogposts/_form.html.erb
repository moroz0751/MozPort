<%= form_with(model: saved_blogpost, local: true) do |form| %>
  <% if saved_blogpost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(saved_blogpost.errors.count, "error") %> prohibited this saved_blogpost from being saved:</h2>

      <ul>
        <% saved_blogpost.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :blogpost_id %>
    <%= form.number_field :blogpost_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
